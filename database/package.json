{
  "name": "database",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "lint": "echo '[database] lint: no linting configured for SQL migrations'",
    "format": "prettier --write '**.sql'"
  },
  "devDependencies": {
    "prettier": "^3.8.1",
    "prettier-plugin-sql": "^0.19.2"
  }
}
